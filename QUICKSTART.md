# 快速开始指南 🚀

## 5分钟快速启动

### 方法一：使用启动脚本（推荐）

#### Windows用户
双击 `run.bat` 文件，或在命令行执行：
```cmd
run.bat
```

#### Linux/Mac用户
在终端执行：
```bash
./run.sh
```

### 方法二：手动启动

#### 1. 安装依赖
```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

#### 2. 启动服务
```bash
cd backend
python app.py
```

#### 3. 访问应用
在浏览器打开：`http://localhost:5000`

---

## 功能体验

### 试试这些问题：

1. **翻译功能**
   ```
   请把"你好"翻译成法语
   ```

2. **发音学习**
   ```
   bonjour怎么发音？
   ```

3. **语法解释**
   ```
   tu和vous有什么区别？
   ```

4. **日常对话**
   ```
   我想学习法语的数字怎么说
   ```

---

## 界面功能说明

### 主要功能

- **聊天区域**：显示对话历史
- **输入框**：输入你的问题
- **发送按钮**：发送消息
- **清除历史**：清空当前对话
- **设置按钮**：配置应用选项

### 快捷操作

- **Enter键**：发送消息
- **Shift + Enter**：输入换行
- **点击示例**：快速输入常用问题

---

## 常见问题

### Q: 启动后无法访问？
**A:** 检查以下几点：
1. 确认端口5000未被占用
2. 检查防火墙设置
3. 尝试访问 `http://127.0.0.1:5000`

### Q: 如何停止服务？
**A:** 在命令行窗口按 `Ctrl + C`

### Q: 如何配置真实的AI API？
**A:**
1. 复制 `.env.example` 为 `.env`
2. 填入你的API密钥
3. 重启服务

### Q: 提示依赖安装失败？
**A:**
1. 确保Python版本 >= 3.8
2. 升级pip: `pip install --upgrade pip`
3. 单独安装失败的包

---

## 下一步

### 集成真实AI（可选）

编辑 `backend/app.py` 中的 `generate_response()` 函数：

```python
def generate_response(message, intent, history):
    # 替换为真实的API调用
    import requests

    response = requests.post(
        "https://api.example.com/chat",
        headers={"Authorization": f"Bearer {API_KEY}"},
        json={"message": message, "history": history}
    )

    return response.json()["reply"]
```

### 添加更多功能

参考 `project_society.md` 了解：
- 如何集成语音识别
- 如何添加语音合成
- 如何实现RAG系统

---

## 技术支持

遇到问题？

1. 查看 [README.md](README.md) 详细文档
2. 查看 [project_society.md](project_society.md) 完整说明
3. 检查 `logs/app.log` 日志文件
4. 提交Issue到项目仓库

---

## 开始学习吧！

一切就绪！🎉

现在你可以：
✅ 与AI老师对话学习法语
✅ 随时翻译中法文
✅ 学习法语发音和语法
✅ 探索更多法语知识

**Bonne chance!** (祝你好运！) 🇫🇷
